#include <main.h>

/**
 * Main - Entry Point 
 * Description: prints the alphabet, in lowercase, followed by a new line
 * Return: 0
 */
void print_alphabet_x10(void)

{
	char c, i;

	for (i = 0, i <= 'z', c++)

	{

		for (c = 'a'; c <= 'z'; c++)
		{
			_putchar(c);
		}
			_putchar('\n');
	}

}
