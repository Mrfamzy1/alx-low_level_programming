#include "main.h"

/**
 * mul -  checks for checks for a digit (0 through 9).
 * @a: a -  Variable
 * @a: b -  Variable
 * Return: Always 0.
 */
int mul(int a, int b)
{

	int mu1;

	mu1 = a * b;

	return (mu1);
}
